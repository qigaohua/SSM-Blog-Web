<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="ssm.blog.dao.IUserDao" >


	
	<sql id="UserColumns">
		id, username, password, profile, nickname, sign, imagename
	</sql>
	
	<select id="getUserDataById" parameterType="int" resultType="ssm.blog.entity.User">
		select <include refid="UserColumns" /> from user where id=#{id}
	</select>
	
	<select id="getUserData" parameterType="String" resultType="ssm.blog.entity.User">
		select <include refid="UserColumns" /> from user where username=#{username}
	</select>

</mapper>



        
        
